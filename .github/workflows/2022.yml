
name: Testing demo
on:
  workflow_dispatch:
  push:
    branches:
      - main
      
  
jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
    # Step 1: Check Out the Code
    - name: Checkout Code
      uses: actions/checkout@v3
      
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    # Step 2: Log in to Azure Container Registry
    - name: Login to ACR
      uses: azure/docker-login@v1
      with:
        login-server: 'byd1370.azurecr.io'
        username: 'BYD1370'
        password: 'f1knUSTqhNlOD3lAMtLsft7h2jRkgEm+f7Drh8tQPz+ACRDHqr0z'

      
    - name: Build and push
      uses: docker/build-push-action@v6
      with:
        push: true
        tags: byd1370.azurecr.io/demo:${{ github.sha }}
        file: ./Dockerfile
        

  
   
